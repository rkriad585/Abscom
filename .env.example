# Abscom has no runtime environment configuration.
# The library reads no environment variables; all configuration is done at
# build time via Meson options and compile-time macros (see docs/configuration.md).
#
# If you add a feature that needs environment configuration, define variables
# here with sensible defaults.
